function executar37(){
    //entrada de dados
    let contadora = 1
    let somaTotal = 0

    //processamento
    while (contadora<100) {
        //saida de dados
        somaTotal += contadora
        alert(parseInt(somaTotal))
        contadora++
    }
}
