function executar40(){
    //entrada de dados
    let numero = parseInt(prompt('Digite um número: '))

    //processamento
    while(contadora<11){
        alert(`${numero} x ${contadora} = ${numero*contadora}`)
    }   
}
