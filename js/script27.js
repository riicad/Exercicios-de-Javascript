function executar27(){
    //entrada de dados
    let idade = parseInt(prompt("Digite sua idade: "))
    let sexo = prompt("Digite seu sexo: M/F ")

    //processamento
    if (sexo.toUpperCase=M){
        if (idade>65){
            //saida de dados
            alert('Ele pode se aposentar!')
        }
        else{
            //saida de dados
            alert('Não pode se aposentar!')
        }
    }
    else{
        if(idade>60){
            //saida de dados
            alert('Ela pode se aposentar!')
        }
        else{
            //saida de dados
            alert('Ela não pode se aposentar!')
        }
    }
}
