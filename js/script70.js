function executar70(){
    //entrada de dados
    let numero = parseInt(prompt('Digite um número: '))
    
    //processamneto
    for(i = 0; i<11; i++){
        //saida de dados
        alert(`${numero} x ${i} = ${numero * i}`)
    }
}
